#!/usr/bin/env python3
import socket
import time

# Sbox from encrypt.py
Sbox = (
    ,
)

def query(plaintext_hex):
    """Query server"""
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect(('saturn.picoctf.net', 54532))
    time.sleep(0.1)
    s.recv(1024)  # prompt
    s.send((plaintext_hex + '\n').encode())
    time.sleep(0.1)
    response = s.recv(1024).decode()
    s.close()
    
    if 'leakage result:' in response:
        return int(response.split('leakage result:')[1].strip())
    return None

print("=" * 60)
print("POWER ANALYSIS ATTACK - IMPROVED")
print("=" * 60)

recovered_key = []

# For each byte position
for pos in range(16):
    print(f"\n[Byte {pos}]")
    
    # Use more test values for better accuracy
    test_values = [0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x10, 0x20, 0x40, 0x80, 0xAA, 0xFF]
    
    # Store observations
    observations = []
    
    for val in test_values:
        pt = [0x00] * 16
        pt[pos] = val
        pt_hex = ''.join(f'{b:02x}' for b in pt)
        
        leak = query(pt_hex)
        if leak is not None:
            observations.append((val, leak))
            print(f"  pt[{pos}]={val:02x} → leak={leak}")
    
    # Find key byte that matches all observations
    candidates = {}
    
    for key_byte in range(256):
        matches = 0
        
        # Check if this key byte is consistent with all observations
        if len(observations) >= 2:
            # Get the baseline (when plaintext[pos] = 0)
            base_val, base_leak = observations[0]
            base_lsb = Sbox[base_val ^ key_byte] & 0x01
            
            # Check all other observations
            for val, leak in observations[1:]:
                expected_lsb = Sbox[val ^ key_byte] & 0x01
                expected_diff = expected_lsb - base_lsb
                actual_diff = leak - base_leak
                
                if expected_diff == actual_diff:
                    matches += 1
            
            candidates[key_byte] = matches
    
    # Find all candidates with maximum score
    if candidates:
        max_score = max(candidates.values())
        best_candidates = [k for k, v in candidates.items() if v == max_score]
        
        if len(best_candidates) == 1:
            best = best_candidates[0]
            print(f"  → Unique solution: {best:02x} (score: {max_score}/{len(observations)-1})")
        else:
            print(f"  → Multiple candidates with score {max_score}: {[f'{c:02x}' for c in best_candidates[:5]]}")
            # Need more tests to disambiguate
            # For now, take the first one
            best = best_candidates[0]
            print(f"  → Using: {best:02x}")
        
        recovered_key.append(best)
    else:
        recovered_key.append(0x00)
        print(f"  → Failed, using 0x00")

print("\n" + "=" * 60)
print("RECOVERED KEY:")
key_hex = ''.join(f'{b:02x}' for b in recovered_key)
print(f"Hex: {key_hex}")
print(f"Flag: picoCTF{{{key_hex}}}")
print("=" * 60)

# Verify by checking with a known plaintext
print("\nVerification:")
pt_test = "00000000000000000000000000000000"
leak_test = query(pt_test)
print(f"All zeros leak: {leak_test}")

# Calculate expected leak
expected = 0
for i in range(16):
    if Sbox[0x00 ^ recovered_key[i]] & 0x01:
        expected += 1
print(f"Expected leak with recovered key: {expected}")

if leak_test == expected:
    print("✓ Verification passed!")
else:
    print("✗ Verification failed - key might be incorrect")